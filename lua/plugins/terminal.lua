return {
  "akinsho/toggleterm.nvim",
  version = "*",
  opts = {
    size = 20,
    open_mapping = [[<C-t>]], -- 改為建議的 Ctrl + t
    direction = "horizontal", -- 改為建議的下方開啟
    shade_terminals = true,
    persist_size = true,
  },
}
